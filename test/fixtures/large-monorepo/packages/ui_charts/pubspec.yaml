name: ui_charts
description: Chart and graph widgets.
version: 1.0.0

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.24.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_utils:
    path: ../core_utils
