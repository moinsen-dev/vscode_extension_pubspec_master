name: melos_hybrid_workspace
description: A workspace using both Melos and Pub Workspaces.

environment:
  sdk: '>=3.6.0 <4.0.0'

workspace:
  - packages/shared
  - packages/feature

dev_dependencies:
  melos: ^6.0.0
