name: data_layer
description: Data layer with additional conflicts.
version: 1.5.0

environment:
  sdk: '>=3.2.0 <4.0.0'

resolution: workspace

dependencies:
  # Another http version
  http: ^1.0.1
  # JSON serialization - no conflict
  json_annotation: ^4.8.0
  # Uses shared_lib
  shared_lib:
    path: ../shared_lib
