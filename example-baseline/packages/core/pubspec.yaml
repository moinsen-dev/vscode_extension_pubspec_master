name: core
description: Core business logic and utilities.
version: 1.0.0

environment:
  # INTENTIONAL MISMATCH: SDK >=3.4.0 (root workspace requires >=3.6.0)
  sdk: '>=3.4.0 <4.0.0'

resolution: workspace

dependencies:
  shared_models:
    path: ../shared_models

  # Utilities
  collection: ^1.18.0
  meta: ^1.11.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
